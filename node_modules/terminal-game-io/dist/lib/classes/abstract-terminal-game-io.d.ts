import { KeyName } from '..';
import { AbstractFrameHandler, AbstractKeypressHandler, IAbstractTerminalGameIo, IAbstractTerminalGameIoOptions } from '../models/abstract-terminal-game-io.interface';
export declare abstract class AbstractTerminalGameIo implements IAbstractTerminalGameIo {
    protected active: boolean;
    protected frameDuration: number;
    protected frameHandler: AbstractFrameHandler;
    protected intervalId: any;
    protected keypressHandler: AbstractKeypressHandler;
    protected currentFrameData: string;
    protected startTime: number;
    protected constructor(options: IAbstractTerminalGameIoOptions);
    drawFrame(data: string, width: number, height: number): void;
    exit(): void;
    getTime(): number;
    triggerKeypress(keyName: KeyName): void;
    protected abstract clear(): void;
    protected abstract finalCleanup(): void;
    protected initialize(): void;
    protected abstract initializeEvents(): void;
    protected abstract write(value: string): void;
}
